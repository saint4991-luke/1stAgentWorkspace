# bridge.py 依賴

# Web 框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# SSE 串流支援
sse-starlette>=2.0.0

# Session 模組依賴（與 session/ 共用）
# 已在 session/requirements.txt 中定義

# ext_query 模組依賴（與 ext_query/ 共用）
# 已在 ext_query/requirements.txt 中定義
